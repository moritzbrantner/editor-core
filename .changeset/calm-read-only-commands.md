---
"@moenarch/editor-core": minor
---

Add an async read-only command conformance helper that proves registered commands mutate in writable mode and are rejected without document mutation in read-only mode.
